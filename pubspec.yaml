name: flutter_login
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '^3.9.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # 🔥 Firebase Core (obligatorio)
  firebase_core: ^3.6.0

  # 👤 Firebase Authentication (para login/registro)
  firebase_auth: ^5.3.1

  # ☁️ Firestore (opcional, si usarás base de datos)
  cloud_firestore: ^5.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
